---
/**
 * WDA-315: Hero component para página de Careers
 * Sin video, con imagen de fondo y CTA de scroll
 */

interface Props {
  title: string; // SEO h1
  heading: string; // Commercial h2
  subtitle: string;
  ctaText: string;
  ctaHref: string;
  backgroundImage: string;
}

const { title, heading, subtitle, ctaText, ctaHref, backgroundImage } = Astro.props;
---

<section class="careers-job-hero">
  <div class="careers-job-hero__overlay"></div>
  <div class="careers-job-hero__background" style={`background-image: url('${backgroundImage}');`}></div>

  <div class="careers-job-hero__content">
    <h1 class="careers-job-hero__title-seo">{title}</h1>
    <h2 class="careers-job-hero__heading">{heading}</h2>
    <p class="careers-job-hero__subtitle">{subtitle}</p>

    <a href={ctaHref} class="careers-job-hero__cta" aria-label={ctaText}>
      <span class="careers-job-hero__cta-text">{ctaText}</span>
      <svg
        class="careers-job-hero__cta-icon"
        width="24"
        height="24"
        viewBox="0 0 24 24"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        aria-hidden="true"
      >
        <path
          d="M12 5L12 19M12 19L19 12M12 19L5 12"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        />
      </svg>
    </a>
  </div>
</section>

<style>
  .careers-job-hero {
    position: relative;
    width: 100%;
    height: 70vh;
    min-height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .careers-job-hero__background {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    z-index: 1;
  }

  .careers-job-hero__overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.3);
    z-index: 2;
  }

  .careers-job-hero__content {
    position: relative;
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 1100px;
    width: 100%;
    padding: 6rem 2rem;
    gap: 1.5rem;
  }

  .careers-job-hero__title-seo {
    font-family: 'Avenir Next', 'Avenir', system-ui, sans-serif;
    font-weight: 300;
    font-size: 0.875rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
  }

  .careers-job-hero__heading {
    font-family: 'Helvetica Neue', 'Helvetica', Arial, sans-serif;
    font-weight: 300;
    font-size: 3rem;
    line-height: 1.2;
    color: #ffffff;
    margin: 0;
    letter-spacing: -0.02em;
  }

  .careers-job-hero__subtitle {
    font-family: 'Avenir Next', 'Avenir', system-ui, sans-serif;
    font-weight: 300;
    font-size: 1.25rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.9);
    margin: 0;
    max-width: 700px;
  }

  .careers-job-hero__cta {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    margin-top: 2rem;
    padding: 1rem;
    color: #ffffff;
    text-decoration: none;
    transition: transform 0.3s ease, opacity 0.3s ease;
    cursor: pointer;
  }

  .careers-job-hero__cta:hover {
    transform: translateY(4px);
    opacity: 0.9;
  }

  .careers-job-hero__cta:focus-visible {
    outline: 2px solid #DB4529;
    outline-offset: 4px;
    border-radius: 4px;
  }

  .careers-job-hero__cta-text {
    font-family: 'Avenir Next', 'Avenir', system-ui, sans-serif;
    font-weight: 400;
    font-size: 0.875rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .careers-job-hero__cta-icon {
    width: 24px;
    height: 24px;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
    60% {
      transform: translateY(-5px);
    }
  }

  /* Mobile responsive (< 768px) */
  @media (max-width: 767px) {
    .careers-job-hero {
      height: 60vh;
      min-height: 450px;
    }

    .careers-job-hero__content {
      padding: 4rem 1.5rem;
      gap: 1rem;
    }

    .careers-job-hero__title-seo {
      font-size: 0.75rem;
    }

    .careers-job-hero__heading {
      font-size: 1.75rem;
      line-height: 1.3;
    }

    .careers-job-hero__subtitle {
      font-size: 1rem;
    }

    .careers-job-hero__cta {
      margin-top: 1.5rem;
    }
  }

  /* Tablet (768px - 1023px) */
  @media (min-width: 768px) and (max-width: 1023px) {
    .careers-job-hero__heading {
      font-size: 2.5rem;
    }

    .careers-job-hero__subtitle {
      font-size: 1.125rem;
    }
  }

  /* Reduced motion for accessibility */
  @media (prefers-reduced-motion: reduce) {
    .careers-job-hero__cta-icon {
      animation: none;
    }

    .careers-job-hero__cta:hover {
      transform: none;
    }
  }
</style>

<script>
  // Smooth scroll al hacer click en CTA
  document.addEventListener('DOMContentLoaded', () => {
    const ctaButtons = document.querySelectorAll('.careers-job-hero__cta');

    ctaButtons.forEach(button => {
      button.addEventListener('click', (e) => {
        const href = button.getAttribute('href');

        if (href && href.startsWith('#')) {
          e.preventDefault();
          const targetId = href.substring(1);
          const targetElement = document.getElementById(targetId);

          if (targetElement) {
            targetElement.scrollIntoView({
              behavior: 'smooth',
              block: 'start'
            });
          }
        }
      });
    });
  });
</script>
