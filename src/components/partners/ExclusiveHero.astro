---
/**
 * WDA-334: Hero exclusivo para Partners B2B
 * Estilo simplificado como /trabaja-con-nosotros/ pero con fondo verde corporativo
 */

interface Props {
  title: string; // SEO h1
  heading: string; // Commercial h2
  subtitle: string;
  ctaText: string;
  ctaHref: string;
  backgroundImage: string; // Mantenido por compatibilidad pero no se usa
}

const { title, heading, subtitle, ctaText, ctaHref } = Astro.props;
---

<section class="partners-hero">
  <div class="partners-hero__content">
    <h1 class="partners-hero__title-seo">{title}</h1>
    <h2 class="partners-hero__heading">{heading}</h2>
    <p class="partners-hero__subtitle">{subtitle}</p>

    <a href={ctaHref} class="partners-hero__cta" aria-label={ctaText}>
      <span class="partners-hero__cta-text">{ctaText}</span>
      <svg
        class="partners-hero__cta-icon"
        width="24"
        height="24"
        viewBox="0 0 24 24"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        aria-hidden="true"
      >
        <path
          d="M12 5L12 19M12 19L19 12M12 19L5 12"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        />
      </svg>
    </a>
  </div>
</section>

<style>
  /* WDA-334: Estilo hero B2B - fondo verde sólido, sin imagen */
  .partners-hero {
    position: relative;
    width: 100%;
    padding: 10rem 0 3rem;
    background: #406E51;
    overflow: hidden;
  }

  .partners-hero__content {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 1100px;
    width: 100%;
    margin: 0 auto;
    padding: 0 2rem;
    gap: 1.5rem;
  }

  /* WDA-334: Jerarquía como /trabaja-con-nosotros/ - h1 pequeño, h2 grande */
  .partners-hero__title-seo {
    font-family: 'Avenir Next', 'Avenir', system-ui, sans-serif;
    font-weight: 400;
    font-size: 18px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.9);
    margin: 0 0 1.5rem 0;
  }

  .partners-hero__heading {
    font-family: 'Helvetica Neue', 'Helvetica', Arial, sans-serif;
    font-weight: 100;
    font-size: 56px;
    line-height: 1.2;
    color: #ffffff;
    margin: 0 0 2.5rem 0;
    letter-spacing: normal;
  }

  .partners-hero__subtitle {
    font-family: 'Avenir Next', 'Avenir', system-ui, sans-serif;
    font-weight: 300;
    font-size: 1.25rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.9);
    margin: 0;
    max-width: 700px;
  }

  .partners-hero__cta {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    margin-top: 2rem;
    padding: 1rem;
    color: #ffffff;
    text-decoration: none;
    transition: transform 0.3s ease, opacity 0.3s ease;
    cursor: pointer;
  }

  .partners-hero__cta:hover {
    transform: translateY(4px);
    opacity: 0.9;
  }

  .partners-hero__cta:focus-visible {
    outline: 2px solid #DB4529;
    outline-offset: 4px;
    border-radius: 4px;
  }

  .partners-hero__cta-text {
    font-family: 'Avenir Next', 'Avenir', system-ui, sans-serif;
    font-weight: 400;
    font-size: 0.875rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .partners-hero__cta-icon {
    width: 24px;
    height: 24px;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
    60% {
      transform: translateY(-5px);
    }
  }

  /* Mobile responsive (< 768px) */
  @media (max-width: 767px) {
    .partners-hero {
      padding: 5.5rem 0 2rem; /* WDA-334: Mismo padding que careers/blog mobile */
    }

    .partners-hero__content {
      padding: 0 1.5rem;
      gap: 2rem;
    }

    .partners-hero__title-seo {
      font-size: 16px;
      letter-spacing: 3px;
      margin-bottom: 0;
    }

    .partners-hero__heading {
      font-size: 32px;
      font-weight: 300;
      line-height: 1.3;
      margin-bottom: 0;
    }

    .partners-hero__subtitle {
      font-size: 1rem;
    }

    .partners-hero__cta {
      margin-top: 1.5rem;
    }
  }

  /* Tablet (768px - 1023px) */
  @media (min-width: 768px) and (max-width: 1023px) {
    .partners-hero__title-seo {
      font-size: 16px;
    }

    .partners-hero__heading {
      font-size: 40px;
    }

    .partners-hero__subtitle {
      font-size: 1.125rem;
    }
  }

  /* Small mobile (<= 640px) */
  @media (max-width: 640px) {
    .partners-hero__title-seo {
      font-size: 14px;
      letter-spacing: 2px;
    }

    .partners-hero__heading {
      font-size: 28px;
      line-height: 1.3;
    }
  }

  /* Reduced motion for accessibility */
  @media (prefers-reduced-motion: reduce) {
    .partners-hero__cta-icon {
      animation: none;
    }

    .partners-hero__cta:hover {
      transform: none;
    }
  }
</style>

<script>
  // Smooth scroll al hacer click en CTA
  document.addEventListener('DOMContentLoaded', () => {
    const ctaButtons = document.querySelectorAll('.partners-hero__cta');

    ctaButtons.forEach(button => {
      button.addEventListener('click', (e) => {
        const href = button.getAttribute('href');

        if (href && href.startsWith('#')) {
          e.preventDefault();
          const targetId = href.substring(1);
          const targetElement = document.getElementById(targetId);

          if (targetElement) {
            targetElement.scrollIntoView({
              behavior: 'smooth',
              block: 'start'
            });
          }
        }
      });
    });
  });
</script>
