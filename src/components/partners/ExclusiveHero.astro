---
/**
 * WDA-528: Hero con imagen de fondo para Partners B2B
 * Mismo formato que careers pero con verde B2B en focus
 */

interface Props {
  title: string; // SEO h1
  heading: string; // Commercial h2
  subtitle: string;
  ctaText: string;
  ctaHref: string;
  backgroundImage: string;
}

const { title, heading, subtitle, ctaText, ctaHref, backgroundImage } = Astro.props;
---

<section class="partners-hero">
  <div class="partners-hero__overlay"></div>
  <div class="partners-hero__background" style={`background-image: url('${backgroundImage}');`}></div>

  <div class="partners-hero__content">
    <h1 class="partners-hero__title-seo">{title}</h1>
    <h2 class="partners-hero__heading">{heading}</h2>
    <p class="partners-hero__subtitle">{subtitle}</p>
  </div>

  <!-- CTA Button positioned at bottom -->
  <a href={ctaHref} class="partners-hero__cta" aria-label={ctaText}>
    <span class="partners-hero__cta-text">{ctaText}</span>
    <svg
      class="partners-hero__cta-icon"
      width="24"
      height="24"
      viewBox="0 0 24 24"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      aria-hidden="true"
    >
      <path
        d="M12 5L12 19M12 19L19 12M12 19L5 12"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
      />
    </svg>
  </a>
</section>

<style>
  /* WDA-547: Estilo hero B2B - imagen de fondo con overlay */
  .partners-hero {
    position: relative;
    width: 100%;
    max-width: 100vw;
    height: 100vh;
    height: 100dvh;
    min-height: 600px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .partners-hero__background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .partners-hero__overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.4) 0%,
      rgba(0, 0, 0, 0.6) 50%,
      rgba(0, 0, 0, 0.7) 100%
    );
    z-index: 1;
  }

  .partners-hero__content {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 1100px;
    width: 100%;
    margin: 0 auto;
    padding: 0 2rem;
    gap: 1.5rem;
  }

  /* WDA-334: Jerarquía como /trabaja-con-nosotros/ - h1 pequeño, h2 grande */
  .partners-hero__title-seo {
    font-family: 'Avenir Next', 'Avenir', system-ui, sans-serif;
    font-weight: 400;
    font-size: var(--font-size-heading);
    letter-spacing: 2px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.9);
    margin: 0 0 1.5rem 0;
  }

  .partners-hero__heading {
    font-family: 'Helvetica Neue', 'Helvetica', Arial, sans-serif;
    font-weight: 100;
    font-size: 56px;
    line-height: 1.2;
    color: #ffffff;
    margin: 0 0 2.5rem 0;
    letter-spacing: normal;
  }

  .partners-hero__subtitle {
    font-family: 'Avenir Next', 'Avenir', system-ui, sans-serif;
    font-weight: 300;
    font-size: var(--font-size-body);
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.9);
    margin: 0;
    max-width: 700px;
  }

  .partners-hero__cta {
    position: absolute;
    bottom: 3rem;
    left: 0;
    right: 0;
    margin: 0 auto;
    width: max-content;
    z-index: 3;
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem;
    color: #ffffff;
    text-decoration: none;
    transition: transform 0.3s ease, opacity 0.3s ease;
    cursor: pointer;
  }

  .partners-hero__cta:hover {
    transform: translateY(4px);
    opacity: 0.9;
  }

  .partners-hero__cta:focus-visible {
    outline: 2px solid var(--color-b2b-light); /* WDA-546: Verde B2B */
    outline-offset: 4px;
    border-radius: 4px;
  }

  .partners-hero__cta-text {
    font-family: 'Avenir Next', 'Avenir', system-ui, sans-serif;
    font-weight: 400;
    font-size: var(--font-size-body-xs);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .partners-hero__cta-icon {
    width: 24px;
    height: 24px;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
    60% {
      transform: translateY(-5px);
    }
  }

  /* Mobile responsive (< 768px) */
  @media (max-width: 767px) {
    .partners-hero {
      min-height: 500px;
      height: 100vh;
      height: 100dvh;
    }

    .partners-hero__content {
      padding: 0 1.5rem;
      gap: 1.5rem;
    }

    .partners-hero__title-seo {
      font-size: var(--font-size-heading);
      letter-spacing: 3px;
      margin-bottom: 0;
    }

    .partners-hero__heading {
      font-size: 32px;
      font-weight: 300;
      line-height: 1.3;
      margin-bottom: 0;
    }

    .partners-hero__subtitle {
      font-size: var(--font-size-body);
    }

    .partners-hero__cta {
      bottom: 2rem;
    }
  }

  /* Tablet (768px - 1023px) */
  @media (min-width: 768px) and (max-width: 1023px) {
    .partners-hero__title-seo {
      font-size: var(--font-size-heading);
    }

    .partners-hero__heading {
      font-size: 40px;
    }

    .partners-hero__subtitle {
      font-size: var(--font-size-body);
    }
  }

  /* Small mobile (<= 640px) */
  @media (max-width: 640px) {
    .partners-hero__title-seo {
      font-size: var(--font-size-body-xs);
      letter-spacing: 2px;
    }

    .partners-hero__heading {
      font-size: 28px;
      line-height: 1.3;
    }
  }

  /* Reduced motion for accessibility */
  @media (prefers-reduced-motion: reduce) {
    .partners-hero__cta-icon {
      animation: none;
    }

    .partners-hero__cta:hover {
      transform: none;
    }
  }
</style>

<script>
  // Smooth scroll al hacer click en CTA
  document.addEventListener('DOMContentLoaded', () => {
    const ctaButtons = document.querySelectorAll('.partners-hero__cta');

    ctaButtons.forEach(button => {
      button.addEventListener('click', (e) => {
        const href = button.getAttribute('href');

        if (href && href.startsWith('#')) {
          e.preventDefault();
          const targetId = href.substring(1);
          const targetElement = document.getElementById(targetId);

          if (targetElement) {
            targetElement.scrollIntoView({
              behavior: 'smooth',
              block: 'start'
            });
          }
        }
      });
    });
  });
</script>
